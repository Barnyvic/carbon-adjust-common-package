export enum PurchaseEventName {
  DISPATCH_PURCHASE = 'purchase.dispatched.AI',
}

export enum PurchaseOptimizationStatus {
  STARTED = 'started',
  FAILED = 'failed',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
}

export enum PurchaseDispatchStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
