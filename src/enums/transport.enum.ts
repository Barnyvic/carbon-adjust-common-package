export enum TransportationDetails {
  Private = 'Private',
  Other = 'Other',
}

export enum ModeOfTransportation {
  Car = 'Car',
  Train = 'Train',
  Bus = 'Bus',
}

export enum OptimizedTripStatus {
  Pending = 'Pending',
  Completed = 'Completed',
  Failed = 'Failed',
}
